// Use ES module syntax since you're using `.mjs`
const nextConfig = {
  env: {
    NEXT_PUBLIC_URL: process.env.NEXT_PUBLIC_URL,
  },
};

// Export the configuration using ES module syntax
export default nextConfig;
